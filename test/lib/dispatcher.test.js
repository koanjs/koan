'use strict';

/**
 * Dependencies
 */
var should = require('should');

describe('Dispatcher', function() {
  it('should work with empty configuration');
  it('should use GET when no method specified');
  it('should use \'index\' when no controller is specified');
  it('should use \'index\' when no action is specified');
  it('should work for specified controller / action');
});
