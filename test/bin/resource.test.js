'use strict';

/**
 * Dependencies
 */
var should = require('should');
var path = require('path');
var generate = require(path.join(process.cwd(), 'bin', 'resource'));

describe('Resource generator', function() {
});
